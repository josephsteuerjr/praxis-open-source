# Example Research Chat

mode: normal
mode_set_by: praxis
mode_reason: разговор идёт по делу, вмешиваться есть смысл
mode_until: 2026-02-01T09:00:00+04:00
disclosure: standard
disclosure_set_by: owner

Публичный чат про retrieval и оценку памяти. Меня позвали как участника, а не как
справочную: короткие реплики по предмету, длинные разборы — только если попросили.

Здесь принято приносить замер вместо мнения. Если у меня замера нет, честнее сказать,
что его нет, чем собрать правдоподобное.

Кого я тут знаю: [[person-example]] — спорит по делу; остальные пока незнакомы.

<!--
МУЛЯЖ профиля комнаты (`memory/rooms/<peer_id>.md`). Настоящие профили не публикуются.

Формат: `# Заголовок`, затем МАШИННАЯ ШАПКА `ключ: значение` без пустых строк, затем её
собственный текст. Первая непустая строка, не похожая на `ключ: значение`, начинает тело —
всё после неё считается прозой, даже если выглядит как ключ.

Что значат ключи:

* `mode` — обычно / наблюдай / тише / замри (`normal|observer|quiet|frozen`). Это ЕЁ
  дисциплина, а не наказание: чем поставила, тем и снимает. «Мертва» режимом не бывает —
  это факт от Telegram.
* `mode_set_by` — кто поставил: `praxis` (сама), `owner` (владелец из пульта). Провенанс
  нужен ровно затем, чтобы она знала, сама ли себя приглушила.
* `mode_reason` — причина словами, обрезается до 200 знаков, и обрез называется вслух.
* `mode_until` — срок; по умолчанию сутки, `ttl_h=0` — без срока. У режима «обычно» срок
  не пишется вовсе: покой — не режим на время.
* `disclosure` — `standard` (визитка как есть) или `open` (к визитке идёт проверяемая
  фактура о ней). Читается ТОЛЬКО в групповой визитке; в личке не меняет ничего.
  У `standard` автора нет — это умолчание, а не чьё-то решение.

Легаси-файлы без шапки читаются как тело целиком: старое не ломаем.
-->
